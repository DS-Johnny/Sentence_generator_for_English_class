
<style>

* {
    box-sizing: border-box;
}

body {
    background: #f7f8fc;
}

.main {
    background: #f7f8fc;
}


/* ================================
   HEADER
================================ */

.page-title {
    margin: 0 0 8px;

    font-family: Arial, sans-serif;

    font-size: 13px;
    font-weight: 700;
    letter-spacing: 2.5px;

    color: #625bf6;

    text-align: center;
}

.page-subtitle {
    margin: 0 0 30px;

    font-family: Arial, sans-serif;

    font-size: 14px;
    color: #7a7d89;

    text-align: center;
}


/* ================================
   CARD
================================ */

.card {
    width: 320px;
    height: 210px;

    margin: 0 auto;

    perspective: 1000px;
}


/* ================================
   PARTE QUE GIRA
================================ */

.card-inner {
    position: relative;

    width: 100%;
    height: 100%;

    transform-style: preserve-3d;

    transition: transform 1s ease;

    border-radius: 22px;
}


/* ================================
   FLIP
================================ */

.card:hover .card-inner {
    transform: rotateY(180deg) rotate(5deg);
}


/* ================================
   FRENTE / VERSO
================================ */

.card-front,
.card-back {
    position: absolute;

    inset: 0;

    width: 100%;
    height: 100%;

    display: flex;

    align-items: center;
    justify-content: center;

    padding: 30px;

    border-radius: 22px;

    overflow: hidden;

    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;

    transform-style: preserve-3d;
}


/* ================================
   FRENTE
================================ */

.card-front {
    z-index: 2;

    background: #ffffff;

    border: 1px solid #e8e9ef;

    box-shadow:
        0 12px 30px rgba(39, 42, 61, 0.08),
        0 2px 6px rgba(39, 42, 61, 0.04);
}

.card-front::before {
    content: "";

    position: absolute;

    top: 0;
    left: 0;

    width: 100%;
    height: 4px;

    background: linear-gradient(
        90deg,
        #625bf6,
        #8b5cf6
    );
}


/* ================================
   VERSO
================================ */

.card-back {
    z-index: 1;

    transform: rotateY(180deg);

    background: linear-gradient(
        135deg,
        #625bf6 0%,
        #7547e8 100%
    );

    border: 1px solid rgba(255,255,255,0.15);

    box-shadow:
        0 12px 30px rgba(98,91,246,0.18);
}


/* ================================
   TEXTO
================================ */

.card-front span,
.card-back span {
    position: relative;

    z-index: 5;

    max-width: 100%;

    font-family: Arial, sans-serif;

    font-size: 28px;
    line-height: 1.2;

    font-weight: 700;

    text-align: center;

    overflow-wrap: break-word;
}


/* Inglês */

.card-front span {
    color: #18191f;
}

.card-front span::before {

    display: block;

    margin-bottom: 14px;

    font-size: 10px;
    font-weight: 800;
    letter-spacing: 2px;

    color: #625bf6;
}


/* Português */

.card-back span {
    color: #ffffff;
}

.card-back span::before {

    display: block;

    margin-bottom: 14px;

    font-size: 10px;
    font-weight: 800;
    letter-spacing: 2px;

    color: rgba(255,255,255,0.75);
}


/* ================================
   STREAMLIT BUTTON
================================ */

div.stButton {
    display: flex;
    justify-content: center;

    margin-top: 30px;
}

div.stButton > button {
    min-width: 150px;

    padding: 13px 25px;

    border: 1px solid #e2e3e9;

    border-radius: 10px;

    background: #ffffff;

    color: #30313a;

    font-family: Arial, sans-serif;

    font-size: 13px;
    font-weight: 700;

    box-shadow:
        0 5px 15px rgba(39,42,61,0.06);

    transition:
        background 0.2s ease,
        color 0.2s ease,
        border-color 0.2s ease,
        transform 0.2s ease;
}

div.stButton > button:hover {
    background: #625bf6;

    color: #ffffff;

    border-color: #625bf6;

    transform: translateY(-2px);
}


/* ================================
   MOBILE
================================ */

@media (max-width: 480px) {

    .card {
        width: 90vw;
        max-width: 320px;
        height: 210px;
    }

    .card-front span,
    .card-back span {
        font-size: 25px;
    }

}

</style>